import * as THREE from 'three';
import {houseModel,reading as r} from './house-model-kit.js';
const TAU=Math.PI*2;
export function createCleaningModel(name){
 if(!['Spin dryer','Vacuum cleaner','Upright vacuum cleaner','Aerosol spray can','Air cleaner','Robot vacuum cleaner','Fire extinguisher'].includes(name))return null;
 const m=houseModel(name),{root,part,box,cylinder,disk,sphere,ring,rod,tube,gear,control,finish,covers}=m;
 if(name==='Spin dryer'){
  const body=part('body','Outer casing','Collects water discharged from the rotating perforated drum.');cylinder(.95,.1,[0,.13,0],'leaf',body);const cover=cylinder(.95,1.6,[0,1,0],'leaf',body);covers.push(cover);
  const drum=part('drum','Perforated drum','Provides inward contact force to the clothes while water can escape through holes.',[0,.2,0]);cylinder(.72,.08,[0,0,0],'metal',drum);for(let i=0;i<30;i++){const a=i*TAU/30;rod([.72*Math.cos(a),0,.72*Math.sin(a)],[.72*Math.cos(a),1.45,.72*Math.sin(a)],.023,'metal',drum);}const clothes=[];for(let i=0;i<6;i++)clothes.push(sphere(.17,[.4*Math.cos(i),.45+i*.13,.4*Math.sin(i)],i%2?'clay':'cream',drum));
  const motor=part('motor','Motor and drive shaft','Supply torque to accelerate and sustain drum rotation.');cylinder(.24,.23,[0,.02,0],'ink',motor);rod([0,0,0],[0,.4,0],.04,'gold',motor);
  const drain=part('drain','Water drain','Removes water collected outside the drum.');tube([[.8,.2,0],[1.2,.2,0],[1.2,0,0]],.055,'blue',drain);
  control('rpm','Drum speed',0,1200,50,600,'rpm','Animation is slowed greatly; the numerical acceleration uses the selected speed.');control('radius','Clothing radius',.1,.3,.025,.2,'m');control('angle','Inspection angle',0,360,5,0,'°');
  return finish((v,t)=>{drum.rotation.y=v.angle*Math.PI/180+t*TAU*v.rpm/600;clothes.forEach((cloth,i)=>{cloth.position.x=v.radius*2*Math.cos(i);cloth.position.z=v.radius*2*Math.sin(i);});const acceleration=(v.rpm*TAU/60)**2*v.radius;return {state:{acceleration},readings:[r('Required inward acceleration',`${acceleration.toFixed(1)} m/s²`),r('Relative to gravity',`${(acceleration/9.81).toFixed(1)} g`),r('Water separation','Water leaves through the perforations'),r('Model boundary','No fabric dynamics, imbalance, bearing forces, or drying efficiency are solved')]};},{animated:true});
 }
 if(name==='Vacuum cleaner'||name==='Upright vacuum cleaner'){
  const body=part('body','Cleaner housing','Contains the collection chamber and fan.');const cover=box([1.1,1.65,.7],[0,1,0],'leaf',body);covers.push(cover);box([1.1,.1,.7],[0,.2,0],'leaf',body);
  const bag=part('bag','Porous dust bag','Air passes through; much of the carried dust remains in the bag.');box([.72,.85,.45],[0,.8,0],'cream',bag);
  const fan=part('fan','Motor-driven fan','Moves air through the cleaner, creating a lower pressure at the intake.',[0,1.5,0]);disk(.32,.08,[0,0,0],'gold',fan);for(let i=0;i<8;i++){const a=i*TAU/8;const blade=box([.12,.3,.08],[.15*Math.sin(a),.15*Math.cos(a),0],'metal',fan);blade.rotation.z=-a;}
  const nozzle=part('nozzle','Floor nozzle and hose','Guides dust-laden air into the collection chamber.');box([1.5,.17,.55],[0,.1,.75],'clay',nozzle);tube([[.55,.22,.7],[1,.7,.4],[.7,1.05,0],[.5,1.05,0]],.09,'wood',nozzle);
  const brush=part('brush','Rotating brush and belt','Agitates carpet fibers so the air stream can carry loosened dirt.',[0,.15,.85],nozzle);rod([-.65,0,0],[.65,0,0],.08,'gold',brush);for(let i=0;i<20;i++){const a=i*1.2;rod([-.6+i*.06,0,0],[-.6+i*.06,.12*Math.cos(a),.12*Math.sin(a)],.016,'ink',brush);}
  const air=part('air','Airflow markers','Colored markers trace the intake, bag, fan, and exhaust sequence.');const particles=Array.from({length:8},(_,i)=>sphere(.025,[0,0,0],'blue',air));
  control('power','Fan setting',0,1,.05,.7,'fraction');control('clog','Filter restriction',0,1,.05,0,'fraction','A normalized resistance comparison, not a commercial suction rating.');
  return finish((v,t)=>{const flow=v.power/(1+4*v.clog);fan.rotation.z=t*TAU*v.power*3;brush.rotation.x=t*TAU*v.power*2;particles.forEach((particle,i)=>{const phase=(t*flow+i/8)%1;particle.position.set(.8*(1-phase),.2+phase*1.6,.7*(1-phase));particle.visible=flow>0;});return {state:{relativeFlow:flow},readings:[r('Relative airflow',`${Math.round(flow*100)}% of reference`),r('Intake pressure','Lower than the room when the fan drives flow'),r('Filter task','Let air through while retaining particles'),r('Brush task','Loosen dirt; the airflow transports it')]};},{animated:true});
 }
 if(name==='Aerosol spray can'||name==='Fire extinguisher'){
  const fire=name==='Fire extinguisher';const shell=part('shell',fire?'Pressure vessel':'Sealed can','Contains liquid and a pressurizing gas.');const body=cylinder(.55,1.65,[0,.98,0],fire?'red':'leaf',shell);covers.push(body);cylinder(.55,.08,[0,.17,0],'metal',shell);cylinder(.55,.1,[0,1.83,0],'metal',shell);
  const contents=part('contents',fire?'Extinguishing agent':'Liquid product','Pressure above the liquid drives it into the dip tube.');cylinder(.5,1.05,[0,.73,0],'blue',contents);
  const tubePart=part('dip-tube',fire?'Discharge tube':'Dip tube','Reaches near the bottom so liquid enters when the valve opens.');rod([0,.22,0],[0,1.96,0],.055,'cream',tubePart);
  const valve=part('valve','Spring-return release valve','Pressing the actuator connects the dip tube to the outlet.');cylinder(.14,.19,[0,1.95,0],'gold',valve);const actuator=box([.38,.1,.28],[0,2.12,0],'clay',valve);
  const nozzle=part('nozzle',fire?'Hose and discharge nozzle':'Spray nozzle',fire?'Directs the discharged agent. Different agent types require different nozzles.':'The small outlet breaks the flow into droplets through shear and expansion.');tube(fire?[[.1,1.98,0],[.8,1.65,0],[.9,.7,0],[1.2,.75,0]]:[[.1,2.02,0],[.6,2.02,0]],.045,'ink',nozzle);
  const gas=part('gas',fire?'Pressurizing gas space':'Propellant space','A pressurized gas pushes on the liquid surface. Some aerosol propellants also exist as liquid, helping maintain pressure as they evaporate.');for(let i=0;i<8;i++)sphere(.035,[Math.cos(i)*.3,1.4+(i%3)*.1,Math.sin(i)*.3],'gold',gas);
  if(fire){const cartridge=part('cartridge','Separate gas cartridge example','Cartridge-operated extinguishers store pressurizing gas separately until activated. Stored-pressure types do not use this extra cartridge.',[-.28,1.25,0]);cylinder(.12,.65,[0,0,0],'gold',cartridge);}
  const spray=part('spray','Discharge markers','Shows flow leaving the open valve, not a droplet-size calculation.');const droplets=Array.from({length:15},()=>sphere(.025,[0,0,0],'blue',spray));
  control('pressed','Actuator',0,1,1,0,'','Release to close the spring valve.',[{value:0,label:'Released'},{value:1,label:'Pressed'}]);control('pressure','Pressure difference',0,4,.1,2,'bar','Illustrative pressure range; not an extinguisher specification.');
  return finish((v,t)=>{const flowing=v.pressed===1&&v.pressure>0;actuator.position.y=2.12-(v.pressed?.055:0);droplets.forEach((drop,i)=>{const a=(t*Math.sqrt(v.pressure/2)+i/15)%1;drop.position.set((fire?1.2:.6)+a*.9,(fire?.75:2.02)+(i%3-1)*a*.2,(i%5-2)*a*.09);drop.visible=flowing;});return {state:{flowing,speed:Math.sqrt(2*v.pressure*1e5/1000)},readings:[r('Valve',v.pressed?'Open':'Closed'),r('Liquid discharge',flowing?'Flowing':'Stopped'),r('Ideal water-like jet speed',`${Math.sqrt(2*v.pressure*1e5/1000).toFixed(1)} m/s`,'Bernoulli reference without nozzle losses; not droplet speed.'),r('Stored energy','The pressurized contents supply discharge energy')]};},{animated:true});
 }
 if(name==='Air cleaner'){
  const body=part('body','Air-cleaner housing','Channels air through the selected collection stages.');box([2.5,.1,1],[0,.15,0],'leaf',body);box([2.5,1.5,.06],[0,.9,-.5],'leaf',body);const cover=box([2.5,1.5,.05],[0,.9,.5],'leaf',body);covers.push(cover);
  const prefilter=part('prefilter','Pre-filter','Catches larger particles before they load the finer stages.');for(let i=0;i<10;i++)rod([-.9,.25,-.4+i*.09],[-.9,1.5,-.4+i*.09],.015,'cream',prefilter);
  const collector=part('collector','Charged collection plates','Charged particles experience electric forces toward collection surfaces.');for(const z of [-.3,-.1,.1,.3])box([1.1,1.1,.025],[0,.85,z],'gold',collector);
  const carbon=part('carbon','Activated-carbon stage','Adsorption can retain some gases; performance depends on the material and gas.');box([.13,1.2,.82],[.82,.85,0],'ink',carbon);
  const charger=part('charger','Ionizing wires and voltage supply','High voltage can create ions that charge particles. A voltage multiplier can build high voltage from a lower alternating supply.');for(const z of [-.25,0,.25])rod([-.65,.3,z],[-.65,1.4,z],.009,'clay',charger);
  const fan=part('fan','Air-moving fan','Supplies the airflow through the filters.',[1.18,.85,0]);ring(.36,.025,[0,0,0],'metal',fan).rotation.y=Math.PI/2;for(let i=0;i<4;i++){const blade=box([.07,.6,.11],[0,0,0],'leaf',fan);blade.rotation.x=i*Math.PI/4;}
  const particles=Array.from({length:12},(_,i)=>sphere(.025,[-1.1,.35+i%4*.25,-.25+i%3*.25],'clay'));
  control('mode','Collection method',0,2,1,1,'','An ionizer without collecting plates is distinct from an electrostatic precipitator.',[{value:0,label:'Mechanical filter'},{value:1,label:'Electrostatic plates'},{value:2,label:'Ionizer without plates'}]);control('voltage','Voltage illustration',0,10,.5,5,'kV','Force example uses 1 cm plate spacing and particle charge 10⁻¹⁶ C.');control('airflow','Air speed through field',.25,2,.25,.5,'m/s','Faster flow leaves less time for electrical drift toward a plate.');control('progress','Follow particles through the cleaner',0,1,.02,0,'');
  const result=finish(v=>{collector.visible=v.mode===1;charger.visible=v.mode!==0;const force=v.mode===1?1e-16*v.voltage*1000/.01:0,drift=force/(6*Math.PI*1.8e-5*1e-6),flight=Math.max(0,Math.min(1,(v.progress-.25)/.5))*.1/v.airflow,hitTime=drift>0?.005/drift:Infinity,hitProgress=.25+hitTime*v.airflow/.1*.5;let capturedCount=0;particles.forEach((particle,i)=>{const startZ=[-.2,0,.2][i%3],targetZ=startZ+.1,displacement=drift*flight*20,captured=v.mode===0?v.progress>=.2/2.3:v.mode===1&&hitProgress<=.75&&v.progress>=hitProgress;if(captured)capturedCount++;particle.position.x=captured?(v.mode===0?-.9:-.55+(hitProgress-.25)/.5*1.1):-1.1+v.progress*2.3;particle.position.y=.35+i%4*.25;particle.position.z=v.mode===1?Math.min(targetZ,startZ+displacement):startZ;});return {state:{force,mode:v.mode,captured:capturedCount,drift},readings:[r('Particles at collection surface',`${capturedCount} of 12 illustrative particles`),r('Collection-field force',`${force.toExponential(1)} N`,'F = qE with 1 cm plate spacing; Stokes drag gives lateral drift.'),r('Field transit length','10 cm; particles start halfway between plates'),r('Airflow effect','Faster air gives particles less time to reach a plate'),r('Boundary','Ideal constant particle charge and perfect sticking; no purification-efficiency claim')]};});result.controls.find(c=>c.key==='voltage').enabledWhen=v=>v.mode===1;return result;

 }
 const room=part('room','Test floor and boundary','The robot uses local sensing to avoid the edge and obstacles.');box([3.4,.06,2.5],[0,.02,0],'cream',room);
 const obstacle=part('obstacle','Obstacle','A raised block provides a simple collision target.');box([.45,.4,.65],[.3,.25,0],'wood',obstacle);
 const dock=part('dock','Charging station','The dock provides electrical contact and a return target.',[-1.3,.12,-.9]);box([.5,.2,.25],[0,0,0],'leaf',dock);
 const robot=part('robot','Robot cleaner','Drive wheels move the body; sensors and a controller choose when to turn.',[-1.1,.2,.5]);cylinder(.25,.2,[0,0,0],'leaf',robot);const wheelAssembly=part('drive','Differential drive wheels','Different left and right speeds turn the robot.',[0,0,0],robot);for(const z of [-.2,.2]){const wheel=cylinder(.075,.06,[0,-.07,z],'ink',wheelAssembly);wheel.rotation.x=Math.PI/2;}
 const sensors=part('sensors','Obstacle and edge sensors','The obstacle detector looks ahead; downward sensors detect a missing floor.',[0,0,0],robot);sphere(.03,[.24,0,0],'gold',sensors);for(const z of [-.13,.13])sphere(.02,[.15,-.1,z],'blue',sensors);
 const brush=part('brush','Side brush','Sweeps nearby dirt toward the intake.',[.18,-.1,.15],robot);for(let i=0;i<3;i++){const a=i*TAU/3;rod([0,0,0],[.18*Math.cos(a),0,.18*Math.sin(a)],.012,'ink',brush);}
 control('seconds','Run time',0,30,.5,0,'s');control('mode','Controller mode',0,1,1,0,'','A local rule-based demonstration, not a map-building navigation system.',[{value:0,label:'Clean and avoid'},{value:1,label:'Return to dock'}]);control('obstacle','Obstacle present',0,1,1,1,'', '',[{value:0,label:'Clear floor'},{value:1,label:'Block on floor'}]);
 return finish(v=>{let x=-1.1,z=.5,angle=0,turning=0,event='Moving forward',docked=false;const dt=.05;for(let i=0;i<v.seconds/dt;i++){if(v.mode===1){const dx=-1.3-x,dz=-.7-z;if(Math.hypot(dx,dz)<.08){docked=true;event='At the charging station';break;}angle=Math.atan2(dz,dx);}if(turning>0){angle+=Math.PI*dt;turning-=dt;event='Turning away from detection';continue;}const nx=x+Math.cos(angle)*.18*dt,nz=z+Math.sin(angle)*.18*dt;const blocked=v.obstacle&&Math.abs(nx-.3)<.5&&Math.abs(nz)<.6,edge=Math.abs(nx)>1.38||Math.abs(nz)>1.03;if(blocked||edge){turning=.65;event=edge?'Edge sensor stops forward travel':'Obstacle sensor stops forward travel';continue;}x=nx;z=nz;event=v.mode?'Heading to dock':'Moving forward';}robot.position.set(x,.2,z);robot.rotation.y=-angle;brush.rotation.y=v.seconds*TAU;obstacle.visible=Boolean(v.obstacle);return {state:{x,z,event,docked},readings:[r('Controller action',event),r('Wheel behavior',turning>0?'Different speeds turn the body':'Matched speeds move straight'),r('Dock contact',docked?'Charging position reached':'Not docked'),r('Navigation model','Deterministic local avoidance on a flat test floor; no SLAM or room coverage claim')]};});
}
