import * as THREE from 'three';
import {houseModel,reading} from './house-model-kit.js';
import {samplePlatformScale,PLATFORM_SCALE_DEFAULTS as D,PLATFORM_SCALE_DOMAINS,PLATFORM_SCALE_CONSTANTS as C} from './platform-scale-physics.js';

export function createPlatformScaleModel(){
 const kit=houseModel('Platform scale'),{root,part,control,finish}=kit,system=part('system','Platform scale','Three rigid levers and two pinned connecting rods compare a secured platform load with a small sliding poise.');system.scale.setScalar(3);
 function box(size,position,color,parent){const mesh=kit.box(size,position,color,parent);mesh.geometry.dispose();mesh.geometry=new THREE.BoxGeometry(...size);return mesh;}
 function shapeMesh(shape,depth,parent,color='metal'){const mesh=box([1,1,1],[0,0,0],color,parent);mesh.geometry.dispose();mesh.geometry=new THREE.ExtrudeGeometry(shape,{depth,bevelEnabled:false,curveSegments:64});mesh.geometry.translate(0,0,-depth/2);mesh.userData.profile=shape.getPoints();return mesh;}
 function annulus(outer,inner,depth,position,parent,color='metal'){const shape=new THREE.Shape(),hole=new THREE.Path();shape.absarc(0,0,outer,0,Math.PI*2,false);hole.absarc(0,0,inner,0,Math.PI*2,true);shape.holes.push(hole);const mesh=shapeMesh(shape,depth,parent,color);mesh.position.set(...position);return mesh;}
 function beamShape(start,end,halfHeight,holes,depth,parent,color){const shape=new THREE.Shape();shape.moveTo(start,-halfHeight);shape.lineTo(end,-halfHeight);shape.absarc(end,0,halfHeight,-Math.PI/2,Math.PI/2,false);shape.lineTo(start,halfHeight);shape.absarc(start,0,halfHeight,Math.PI/2,Math.PI*1.5,false);shape.closePath();for(const x of holes){const hole=new THREE.Path();hole.absarc(x,0,.0086,0,Math.PI*2,true);shape.holes.push(hole);}return shapeMesh(shape,depth,parent,color);}
 function movingRod(length,parent){const shape=new THREE.Shape();shape.moveTo(-.025,0);shape.lineTo(-.025,length);shape.absarc(0,length,.025,Math.PI,0,true);shape.lineTo(.025,0);shape.absarc(0,0,.025,0,-Math.PI,true);shape.closePath();for(const y of [0,length]){const hole=new THREE.Path();hole.absarc(0,y,.009,0,Math.PI*2,true);shape.holes.push(hole);}return shapeMesh(shape,.020,parent,'gold');}
 const base=part('base','Rigid frame and pivot bearings','Three fixed pivot centers carry bored bearings. The lower shaft turns with the lever, flywheel, and damper rotor; the frame reacts the forces and depth-offset moments.',[0,0,0],system),floor=box([1.5,.03,.86],[.57,-.255,-.04],'leaf',base),frameMembers=[];
 for(const [x,top] of [[0,.55],[.8,1.07]])frameMembers.push(box([.05,top+.24,.05],[x,(top-.24)/2,-.40],'wood',base));
 for(const y of [-.16,1.04])frameMembers.push(box([.85,.035,.05],[.40,y,-.40],'wood',base));
 function pivot(x,y,end,start=-.425){const shaft=kit.rod([x,y,start],[x,y,end],.0085,'ink',base),bearing=annulus(.024,.0086,.036,[x,y,-.065],base),support=box([.040,.032,.375],[x,y-.040,-.2375],'wood',base);return {center:[x,y,0],shaft,bearing,support};}
 const lowerPivot=pivot(0,0,.31,-.338),upperPivot=pivot(C.upperX,C.upperY,.027),beamPivot=pivot(0,.5,.021);lowerPivot.support.geometry.dispose();lowerPivot.support.geometry=new THREE.BoxGeometry(.040,.216,.040);lowerPivot.support.position.set(0,-.132,-.065);
 const lower=part('lower','Lower lever','The captured platform pin lies 0.20 m from the pivot; the first connecting rod lies 1.00 m away. The output moves five times as far near level.',[0,0,0],system),lowerBody=beamShape(0,1,.023,[0,.2,1],.028,lower,'wood');lower.add(lowerPivot.shaft);const lowerShaftKey=box([.006,.008,.026],[.009,0,0],'metal',lower);
 const upper=part('upper','Upper reversing lever','A 0.20 m input arm and a 0.40 m output arm reverse the motion, doubling output travel and halving the near-level transmitted force.',[C.upperX,C.upperY,0],system),upperBody=beamShape(-.4,.2,.023,[-.4,0,.2],.028,upper,'wood');
 const beam=part('beam','Calibrated weighing beam','The second rod acts 0.40 m from the pivot. Moving the poise changes its torque arm; the bar carries fixed markings for a 5 kg poise.',[0,.5,0],system),beamBody=beamShape(0,1.25,.020,[0,.4],.045,beam,'wood');
 function joint(parent,x,depth,barHalf){const side=Math.sign(depth),inside=side*barHalf,rodFace=depth-side*.010,outerFace=depth+side*.010,shaft=kit.rod([x,0,-side*barHalf],[x,0,outerFace+side*.012],.0085,'ink',parent),boss=annulus(.017,.0086,Math.abs(rodFace-inside),[x,0,(inside+rodFace)/2],parent),washer=annulus(.016,.0086,.006,[x,0,outerFace+side*.003],parent);return {shaft,boss,washer,local:[x,0,depth],depth};}
 const lowerJoint=joint(lower,1,.18,.014),upperRightJoint=joint(upper,.2,.18,.014),upperLeftJoint=joint(upper,-.4,-.18,.014),beamJoint=joint(beam,.4,-.18,.0225);
 const rodOne=part('rod-one','First connecting rod','A rigid 1.00 m rod links the lower lever to the short arm of the upper lever. Its front depth plane clears the weighing beam.',[1,0,.18],system),rodOneBody=movingRod(1,rodOne);
 const rodTwo=part('rod-two','Second connecting rod','A rigid 0.50 m rod links the long upper arm to the weighing beam. Its rear depth plane clears the other moving bars.',[.4,.5,-.18],system),rodTwoBody=movingRod(.5,rodTwo);
 const platform=part('platform','Guided platform and captured pin','A roller is captured between both horizontal slot faces. Two long bored guide collars keep the platform level while the roller moves sideways by a small amount.',[0,0,0],system);
 const platformProfile=new THREE.Shape();platformProfile.moveTo(-.03,-.33);platformProfile.lineTo(.43,-.33);platformProfile.lineTo(.43,.13);platformProfile.lineTo(-.03,.13);platformProfile.closePath();
 const guideRods=[],guideCollars=[];for(const x of [.06,.34]){const hole=new THREE.Path();hole.absarc(x,.10,.0121,0,Math.PI*2,true);platformProfile.holes.push(hole);guideRods.push(kit.cylinder(.012,.44,[x,-.02,-.10],'ink',base));const collar=annulus(.020,.0121,.070,[x,.1355,-.10],platform);collar.rotation.x=Math.PI/2;guideCollars.push(collar);}
 const platformDeck=shapeMesh(platformProfile,.022,platform,'clay');platformDeck.rotation.x=-Math.PI/2;platformDeck.position.y=.18;
 const slotUpper=box([.150,.016,.026],[.2,.020,.10],'metal',platform),slotLower=box([.150,.016,.026],[.2,-.020,.10],'metal',platform),slotEnds=[.117,.283].map(x=>box([.016,.056,.026],[x,0,.10],'metal',platform)),platformPosts=[.14,.26].map(x=>box([.016,.141,.020],[x,.0985,.10],'metal',platform));
 const loadPin=kit.rod([.2,0,-.014],[.2,0,.120],.0085,'ink',lower),loadBoss=annulus(.015,.0086,.072,[.2,0,.050],lower),loadWasher=annulus(.014,.0086,.006,[.2,0,.117],lower),roller=annulus(.012,.0086,.028,[.2,0,.1],lower,'gold');
 const load=part('load','Secured payload','The load is clamped to the platform. This chosen restraint transmits the downward impulse needed when an upper-stop impact arrests the rising payload.',[.2,.271,.1],platform),payload=box([.18,.16,.18],[0,0,0],'blue',load),clamps=[];
 for(const x of [-.100,.100]){clamps.push(box([.012,.185,.026],[x,.0085,0],'metal',load));clamps.push(box([.034,.012,.035],[x,-.083,0],'metal',load));}
 const clampBridge=box([.212,.014,.030],[0,.087,0],'metal',load);clamps.push(clampBridge);
 const poise=part('poise','Sliding poise and lock','A rear-open carriage passes the beam pivot and rod pin. Its equal upper and lower blocks represent a compact mass centered at the selected graduation.',[D.poise,0,0],beam),poiseMeshes=[];
 for(const sign of [-1,1]){poiseMeshes.push(box([.060,.008,.101],[0,sign*.024,.029],'metal',poise));poiseMeshes.push(box([.060,.054,.100],[0,sign*.055,.035],'gold',poise));}
 const poiseBridge=box([.060,.080,.025],[0,0,.0675],'gold',poise),poiseLock=kit.rod([.020,0,.0225],[.020,0,.091],.004,'ink',poise),poiseKnob=kit.disk(.012,.009,[.020,0,.095],'gold',poise);poiseMeshes.push(poiseBridge,poiseLock,poiseKnob);
 const graduations=part('graduations','Fixed mass graduations','The bar reads 125 kilograms per meter of poise distance for the nominal 5 kg poise. Actual poise-mass changes do not redraw this scale.',[0,0,0],beam),markPoints=[];
 for(let i=0;i<=30;i++){const x=i*.04;markPoints.push(new THREE.Vector3(x,.018,.0228),new THREE.Vector3(x,i%5===0?-.015:.004,.0228));}
 const scaleMarks=new THREE.LineSegments(new THREE.BufferGeometry().setFromPoints(markPoints),new THREE.LineBasicMaterial({color:0x374736}));graduations.add(scaleMarks);
 const segments=[[[0,1],[.6,1]],[[.6,1],[.6,.5]],[[.6,.5],[.6,0]],[[0,0],[.6,0]],[[0,.5],[0,0]],[[0,1],[0,.5]],[[0,.5],[.6,.5]]],digitPaths={0:[0,1,2,3,4,5],1:[1,2],2:[0,1,6,4,3],3:[0,1,6,2,3],4:[5,6,1,2],5:[0,5,6,2,3],6:[0,5,6,4,2,3],7:[0,1,2],8:[0,1,2,3,4,5,6],9:[0,1,2,3,5,6]},numberLabels=[];
 for(let kg=0;kg<=150;kg+=25){const text=String(kg),points=[],label=new THREE.Group();label.position.set(kg/125,.031,.023);[...text].forEach((character,index)=>{for(const segment of digitPaths[character])for(const [x,y] of segments[segment])points.push(new THREE.Vector3((x+index*.8-text.length*.4)*.020,y*.020,0));});label.add(new THREE.LineSegments(new THREE.BufferGeometry().setFromPoints(points),new THREE.LineBasicMaterial({color:0x374736})));label.userData.mass=kg;graduations.add(label);numberLabels.push(label);}
 const poiseIndex=kit.rod([0,.095,.035],[0,.080,.035],.0025,'red',poise);
 const stops=part('stops','Two travel stops','The rounded lower-lever lug meets a horizontal stop face at either travel limit. A stop-supported tilted mechanism is still imbalanced.',[0,0,0],system),stopRadius=.012,face=C.stopRadius*Math.sin(C.maximumAngle)+stopRadius,stopMeshes=[];
 for(const sign of [-1,1]){stopMeshes.push(box([.060,.020,.040],[C.stopRadius*Math.cos(C.maximumAngle),sign*(face+.010),.045],'red',stops));stopMeshes.push(box([.040,.020,.020],[.850,sign*(face+.010),.045],'metal',stops));}
 const stopSupport=box([.020,.300,.020],[.84,-.090,.045],'metal',stops),lugStem=kit.rod([.9,0,.014],[.9,0,.045],.008,'metal',lower),stopLug=kit.sphere(stopRadius,[C.stopRadius,0,.045],'gold',lower);
 const level=part('level','Beam level reference','The fixed red datum marks the end height of a level weighing beam. A tilted beam held at a travel stop is not a valid balance reading.',[0,0,0],base);
 const levelDatum=box([.010,.003,.060],[1.284,.5,0],'red',level),levelStand=box([.025,.795,.030],[1.302,.1575,-.03],'wood',base),datumTie=box([.036,.010,.035],[1.293,.50,-.015],'metal',level);
 const flywheel=part('flywheel','Lower-shaft flywheel','This explicit flywheel supplies the assumed 0.05 kg m² inertia in the released-motion model.',[0,0,.28],lower),flywheelBody=annulus(.105,.0086,.024,[0,0,0],flywheel,'metal'),flywheelKey=box([.006,.008,.024],[.009,0,0],'metal',flywheel);
 for(let i=0;i<6;i++){const angle=i*Math.PI/3;kit.rod([.020*Math.cos(angle),.020*Math.sin(angle),.013],[.094*Math.cos(angle),.094*Math.sin(angle),.013],.003,'gold',flywheel);}
 const damper=part('damper','Rotary viscous damper','The casing is fixed to the frame and the inner vane rotates with the lower shaft. Its assumed torque is −20 times angular speed, dissipating mechanical energy as heat.',[0,0,0],system),damperHousing=annulus(.070,.049,.080,[0,0,-.285],damper,'blue'),damperFoot=box([.080,.171,.080],[0,-.1545,-.285],'blue',damper),damperRotor=new THREE.Group();damperRotor.position.z=-.285;damper.add(damperRotor);
 const damperHub=annulus(.020,.0086,.066,[0,0,0],damperRotor,'metal'),damperKey=box([.006,.008,.066],[.009,0,0],'metal',damperRotor),damperVane=box([.030,.013,.060],[.033,0,0],'metal',damperRotor),damperEnds=[-.330,-.240].map(z=>annulus(.070,.0086,.010,[0,0,z],damper,'blue'));kit.covers.push(...damperEnds);
 const loadArrow=new THREE.ArrowHelper(new THREE.Vector3(0,-1,0),new THREE.Vector3(),.16,0xc27a3f,.035,.014),poiseArrow=new THREE.ArrowHelper(new THREE.Vector3(0,-1,0),new THREE.Vector3(),.10,0xc27a3f,.025,.012);system.add(loadArrow,poiseArrow);for(const object of [loadArrow,poiseArrow])object.userData.explosionExcluded=true;
 const specs={mass:['Secured platform mass','kg','Changing a control samples a fresh release from level at the current displayed trial time. The payload remains clamped to the guided platform.'],poise:['Poise position','m','Slide the locked poise along the fixed graduations. Farther from the pivot gives a larger opposing torque.'],poiseMass:['Actual poise mass','kg','The printed bar remains calibrated for 5 kg. A 4 or 6 kg poise changes the balance without changing the markings.',[4,5,6].map(value=>({value,label:value+' kg'}))],gravity:['Gravity','','Equal local gravity acts on both masses. It changes forces and timing while canceling from the level mass comparison.',[{value:0,label:'Earth · 9.81 m/s²'},{value:1,label:'Moon · 1.62 m/s²'}]]};
 for(const [key,[min,max,step]] of Object.entries(PLATFORM_SCALE_DOMAINS)){const [label,unit,help,options]=specs[key];control(key,label,min,max,step,D[key],unit,help,options);}
 function pose(s,values=s.values){
  lower.rotation.z=s.lowerAngle;upper.rotation.z=s.upperAngle;beam.rotation.z=s.beamAngle;platform.position.y=s.platformTravel;poise.position.x=values.poise;
  rodOne.position.set(...s.lowerOutput,.18);rodOne.rotation.z=Math.atan2(s.upperInput[1]-s.lowerOutput[1],s.upperInput[0]-s.lowerOutput[0])-Math.PI/2;rodTwo.position.set(...s.beamInput,-.18);rodTwo.rotation.z=Math.atan2(s.upperOutput[1]-s.beamInput[1],s.upperOutput[0]-s.beamInput[0])-Math.PI/2;
  damperRotor.rotation.z=s.lowerAngle;payload.visible=values.mass>0;
  loadArrow.position.set(.2,.49+s.platformTravel,.1);loadArrow.visible=values.mass>0;loadArrow.setLength(.10+.08*values.mass/150,.035,.014);poiseArrow.position.set(values.poise*Math.cos(s.beamAngle),.5+values.poise*Math.sin(s.beamAngle)+.24,.09);poiseArrow.setLength(.12+.025*(values.poiseMass-4),.025,.012);
 }
 let elapsed=0,lastClock=0,disposed=false;const format=(n,digits=2)=>(Math.abs(n)<.5*10**-digits?0:n).toFixed(digits);
 const result=finish(values=>{
  const s=samplePlatformScale(values,elapsed);pose(s,values);
  const outcome=s.atStop===-1?'The load side has reached the lower stop. The beam is still imbalanced; the stop now supplies the missing torque.':s.atStop===1?'The poise side has driven the mechanism to the upper stop. The platform has stopped rising, but this is not balance.':s.equilibrium?'The released mechanism stays level without a stop reaction. The load and poise torques balance.':s.lowerSpeed<0||s.gravityTorque<0?'The load drives the platform downward and the weighing beam upward. The selected poise does not yet balance the load.':'The poise drives the platform upward and the weighing beam downward. It supplies the larger opposing torque.';
  return {state:s,readings:[
   reading('Outcome',outcome,'A free level balance needs no stop reaction. Motionlessness at either stop still means the load and poise are mismatched.'),
   reading('Fixed-bar indication',format(s.indicatedMass)+' kg','Printed graduation at the poise position. It assumes a 5 kg poise and is a valid load reading only at a free level balance.'),
   reading('Mass balanced by this poise',format(s.balancedMass)+' kg','Actual poise mass × position × 25 at level. A 4 or 6 kg poise changes this value without changing the printed bar.'),
   reading('Required poise position',format(s.requiredPoise,3)+' m','Position needed for the selected payload and actual poise mass. Compare it with the available 0–1.2 m bar.'),
   reading('Balance within bar range',s.canBalance?'Yes':'No','Whether the required position lies on the bar. The control uses 0.04 m steps, so an in-range position may still lie between selectable marks.'),
   reading('Stop torque',format(s.stopTorque)+' N m','Reaction about the lower shaft that holds an imbalanced mechanism at a stop. Positive acts toward raising the platform; zero is required for free balance.'),
   reading('Gravity',format(s.gravity)+' m/s²','Acts equally on payload and poise. It changes forces and release timing, but cancels from the level mass comparison.'),
   reading('Platform displacement upward',format(s.platformTravel*1000,3)+' mm','Signed movement from the level release. Negative means down; the total travel is limited to about 4 mm each way.'),
   reading('Poise displacement upward',format(values.poise*Math.sin(s.beamAngle)*1000,3)+' mm','Signed vertical movement of the poise. The lever train turns a tiny platform movement into this more visible motion.'),
   reading('Weighing beam angle',format(s.beamAngle*180/Math.PI)+'°','Zero aligns the beam with the fixed level datum. Use Inspect level reference to compare their heights.'),
   reading('First rod tension',format(s.firstRodForce)+' N','Pull transmitted between the lower lever and the short upper arm. Its moving value includes acceleration, not just static weight.'),
   reading('Second rod tension',format(s.secondRodForce)+' N','Pull from the upper lever to the weighing beam. At the balanced 100 kg Earth trial it is 98.1 N.'),
   reading('Continuous secured-payload force',format(s.payloadForce)+' N','Upward support equals mass × (gravity + vertical acceleration). This finite force is separate from the brief stopping impulse.'),
   reading('Damper torque',format(s.damperTorque)+' N m','Opposes lower-shaft motion with 20 N m s/rad viscous damping. It vanishes when the mechanism is still.'),
   reading('Signed stop force upward',format(s.stopForce)+' N','Force at the travel-stop lug. Positive is upward at the lower stop; negative is downward at the upper stop.'),
   reading('Platform-guide couple',format(s.guideCouple[2],3)+' N m','The guides balance the small horizontal offset between the payload and captured roller, keeping the platform level without doing work.'),
   reading('Stop impacts',String(s.stopHits),'Counts fully inelastic contacts in this release. A balanced trial has no impacts.'),
   reading('Last impact physical time',s.stopHits?format(s.lastImpactPhysicalTime,3)+' s':'No impact','Actual elapsed release time at contact. Playback takes four times as long so the motion can be followed.'),
   reading('Payload stopping impulse',format(s.payloadStoppingImpulse,3)+' N s','Momentum change delivered through the restraint at impact. Negative means downward when a rising payload stops; this is not a continuous force.'),
   reading('Kinetic energy',format(s.kineticEnergy,3)+' J','Energy in the moving payload, point-mass poise and stated lower-shaft inertia. It is zero after a fully inelastic stop.'),
   reading('Potential energy change',format(s.potentialEnergy,3)+' J','Combined gravitational energy relative to the level release. Its loss becomes kinetic energy and heat.'),
   reading('Damper heat',format(s.damperHeat,3)+' J','Energy dissipated by viscous resistance during motion. It stays recorded after the mechanism stops.'),
   reading('Impact heat',format(s.impactHeat,3)+' J','Incoming kinetic energy lost at a fully inelastic stop. Together with damper heat and kinetic energy it balances the potential-energy loss.'),
   reading('Physical elapsed time',format(s.physicalTime)+' s','Two seconds of actual release are displayed over eight seconds of trial time.'),
   reading('Trial time',format(s.elapsed)+' s','Observation time in the slowed eight-second trial. Presets and Reset start a fresh release from level.'),
  ]};
 });
 const render=result.update;result.advance=dt=>{if(Number.isFinite(dt)&&dt>0)elapsed=Math.min(C.duration,elapsed+dt);return render();};result.animate=time=>{const dt=Number.isFinite(time)?Math.max(0,time-lastClock):0;if(Number.isFinite(time))lastClock=time;return result.advance(dt);};result.reset=()=>{elapsed=lastClock=0;return render(result.defaults);};
 result.actions=[['Inspect start (0%)',0],['Inspect quarter (25%)',.25],['Inspect three quarters (75%)',.75],['Inspect result (100%)',1]].map(([label,p])=>({label,part:'system',view:'front',replay:false,run(){elapsed=C.duration*p;return render();}}));
 result.actions.push({label:'Inspect travel stop',part:'stops',view:'front',replay:false,run:()=>render()},{label:'Inspect level reference',part:'level',view:'front',replay:false,run:()=>render()},{label:'Inspect whole scale',part:'system',view:'front',replay:false,run:()=>render()});
 result.playback={label:'Release the scale from level',description:'Inspect travel stop magnifies the actual stop contact; Inspect level reference compares the beam end with the red datum. These views preserve the selected trial. Two physical seconds of released motion are shown over eight seconds. The flywheel, viscous damper, secured load and fully inelastic stops are explicit teaching assumptions.',stepLabel:'Advance the scale by one percent of the trial',advance:result.advance,step:()=>result.advance(C.duration/100),complete:()=>elapsed>=C.duration,blocked:()=>false};
 result.resultPart={id:'beam',context:'system',label:'Inspect balance and the level reference',view:'front',focusOnComplete:false,available:()=>elapsed>=C.duration};
 result.frameBoundsForPart=id=>{system.updateWorldMatrix(true,true);if(id==='stops')return new THREE.Box3(new THREE.Vector3(.82,-.25,.01),new THREE.Vector3(.96,.075,.08)).applyMatrix4(system.matrixWorld);if(id==='level')return new THREE.Box3(new THREE.Vector3(1.18,.34,-.06),new THREE.Vector3(1.33,.66,.05)).applyMatrix4(system.matrixWorld);if(!['system','platform','load','beam','poise'].includes(id))return null;return new THREE.Box3(new THREE.Vector3(-.19,-.28,-.48),new THREE.Vector3(1.34,1.12,.43)).applyMatrix4(system.matrixWorld);};
 root.rotation.set(.08,-.28,0);result.initialPart='system';result.initialView='front';result.frameVisibleOnly=true;result.framePadding=.5;result.autoFramePart='system';result.selectionOutline=false;result.transparentBackground=true;
 for(const id of ['stops','level']){const detail=result.parts.find(p=>p.id===id);detail.maxZoom=40;detail.framePadding=.65;}
 result.catalogParts=result.parts.filter(p=>!['system','graduations'].includes(p.id));
 result.topology={pose,system,base,floor,frameMembers,lowerPivot,upperPivot,beamPivot,lower,lowerBody,lowerShaftKey,upper,upperBody,beam,beamBody,lowerJoint,upperRightJoint,upperLeftJoint,beamJoint,rodOne,rodOneBody,rodTwo,rodTwoBody,platform,platformDeck,guideRods,guideCollars,slotUpper,slotLower,slotEnds,platformPosts,loadPin,loadBoss,loadWasher,roller,load,payload,clamps,clampBridge,poise,poiseMeshes,poiseBridge,poiseLock,poiseKnob,poiseIndex,graduations,scaleMarks,numberLabels,stops,stopMeshes,stopSupport,lugStem,stopLug,stopRadius,stopFace:face,level,levelDatum,levelStand,datumTie,flywheel,flywheelBody,flywheelKey,damper,damperHousing,damperFoot,damperRotor,damperHub,damperKey,damperVane,damperEnds,loadArrow,poiseArrow};
 const dispose=result.dispose;result.dispose=()=>{if(!disposed){disposed=true;dispose();}};return result;
}
