import assert from 'node:assert/strict';
import * as THREE from 'three';
import {createElectromagnetModel} from './electromagnet-model.js';

const close=(a,b,tolerance=1e-8,label='values')=>assert.ok(Math.abs(a-b)<=tolerance,`${label}: ${a} vs ${b}`);
const objects=model=>Object.fromEntries(model.parts.map(part=>[part.id,part.object]));
function geometry(model){
 const s=model.getState(),parts=objects(model);model.root.updateMatrixWorld(true);
 model.root.traverse(object=>{assert.ok(object.matrixWorld.elements.every(Number.isFinite),'finite transforms');if(object.geometry?.attributes.position)assert.ok(object.geometry.attributes.position.array.every(Number.isFinite),'finite geometry');});
 const pole=new THREE.Box3().setFromObject(parts['central-pole']),outer=new THREE.Box3().setFromObject(parts['outer-pole']),load=new THREE.Box3().setFromObject(parts.load),spacer=new THREE.Box3().setFromObject(parts['surface-spacer']);
 close((pole.min.y-load.max.y)/25,s.gap,1e-8,'central pole/load gap');close((outer.min.y-load.max.y)/25,s.gap,1e-8,'annular pole/load gap');close((spacer.min.y-load.max.y)/25,s.separation,1e-8,'physical face separation');assert.ok(spacer.min.y>=load.max.y-1e-7,'no magnet/load penetration');
 close(pole.getSize(new THREE.Vector3()).x/50,.015,1e-8,'central pole radius matches force area');
 const shell=parts['outer-pole'].children[0].geometry.parameters.points;close(Math.min(...shell.map(p=>p.x))/25,.03);close(Math.max(...shell.map(p=>p.x))/25,.04);
 const leadMeshes=parts['flex-leads'].children;assert.equal(leadMeshes.length,2);
 leadMeshes.forEach((mesh,i)=>{const side=i?1:-1,curve=mesh.geometry.parameters.path,start=curve.getPoint(0),end=curve.getPoint(1);close(curve.getLength(),s.leadLength,1e-6,'fixed service-wire length');assert.ok(start.distanceTo(new THREE.Vector3(side*.55,3.2,.07))<1e-8);assert.ok(end.distanceTo(new THREE.Vector3(side*.55,parts.magnet.position.y+1.18,.07))<1e-8,'moving terminal endpoint');const terminal=parts[i?'positive-terminal':'negative-terminal'].children.find(o=>o.geometry?.type==='CylinderGeometry');const terminalBounds=new THREE.Box3().setFromObject(terminal);close(end.y,terminalBounds.max.y,1e-7,'wire meets terminal top');});
 const suspension=new THREE.Box3().setFromObject(parts.cable);close(suspension.max.y,3.2,1e-8,'cable enters hoist');close(suspension.min.y,parts.magnet.position.y+1.39,1e-8,'cable meets ferrule');assert.ok(suspension.getSize(new THREE.Vector3()).y>.1);
 const eye=parts['lifting-eye'];const pin=parts.shackle.children[2];const pinTop=new THREE.Box3().setFromObject(pin).max.y;close(pinTop,parts.magnet.position.y+1.17+.08-.016,1e-7,'shackle pin bears on eye inner surface');
 assert.equal(parts.field.visible,Math.abs(s.current)>.005,'powered field indicator survives nonmagnetic material');
}
function validState(model){const s=model.getState();for(const key of ['current','loadY','loadVelocity','hoistY','hoistVelocity','hoistAcceleration','force','weight','requiredForce','gap','separation'])assert.ok(Number.isFinite(s[key]),key);assert.ok(s.force>=0,'magnetic force is attractive for either current direction');assert.ok(s.loadY>=0&&s.separation>=0,'no penetration');assert.ok(Math.abs(s.hoistAcceleration)<=.800000001,'bounded hoist acceleration');if(s.held){assert.ok(s.contact,'holding requires actual face contact');assert.ok(s.force+1e-8>=s.requiredForce,'holding includes m(g+a)');}return s;}
function run(model,fps=60,maxSeconds=30){let sample=0;for(let i=0;i<maxSeconds*fps;i++){const s=validState(model);if(s.complete||s.blocked)return s;model.advance(1/fps);if(i===0||(++sample%Math.round(fps*.5)===0))geometry(model);}assert.fail('action did not finish or block');}
function create(settings={}){const model=createElectromagnetModel();model.update(settings);geometry(model);return model;}
const all=[];
const limits=create();all.push(limits);for(const control of limits.controls){for(const value of control.options?control.options.map(o=>o.value):[control.min,control.max]){limits.reset();limits.update({[control.key]:value});validState(limits);geometry(limits);}}const maximum=create({current:3,mass:2,height:50});all.push(maximum);run(maximum);assert.ok(maximum.getState().held);close(maximum.getState().hoistY,.05);assert.equal(maximum.getState().hoistVelocity,0);assert.equal(maximum.getState().hoistAcceleration,0);
const normal=create();all.push(normal);assert.equal(normal.getState().current,0);assert.equal(normal.getState().held,false);const normalDone=run(normal);assert.ok(normalDone.held&&normalDone.complete);assert.ok(normalDone.loadY>.0419);assert.ok(normalDone.force>normalDone.weight);geometry(normal);
const winding=objects(normal).coil.children.filter(o=>o.userData.windingPaths);assert.equal(winding.length,2,'winding requires only two draw calls');assert.equal(winding.filter(mesh=>normal.covers.includes(mesh)).length,1,'front half removed; back retained');const paths=[];for(let i=0;i<200;i++){paths.push(winding[0].userData.windingPaths[i],winding[1].userData.windingPaths[i]);}assert.equal(paths.length,400,'200 complete winding turns');for(let layer=0;layer<10;layer++)for(let half=0;half<39;half++)assert.ok(paths[layer*40+half].getPoint(1).distanceTo(paths[layer*40+half+1].getPoint(0))<1e-10,'joined half-turn endpoints');assert.equal(winding.reduce((count,mesh)=>count+mesh.geometry.attributes.position.count,0),400*40*9,'all original winding vertices preserved');
const stableLeadGeometry=objects(normal)['flex-leads'].children.map(mesh=>mesh.geometry);normal.update({height:30});normal.advance(.1);objects(normal)['flex-leads'].children.forEach((mesh,i)=>assert.equal(mesh.geometry,stableLeadGeometry[i],'moving leads reuse geometry buffers'));normal.update({height:40});run(normal);
const oldY=normal.getState().loadY,oldH=normal.getState().hoistY;normal.update({operation:1});close(normal.getState().loadY,oldY);close(normal.getState().hoistY,oldH);assert.ok(!normal.getState().complete);const releaseDone=run(normal);assert.ok(releaseDone.complete&&!releaseDone.held);assert.equal(releaseDone.loadY,0);assert.ok(Math.abs(releaseDone.current)<.001);geometry(normal);
for(const settings of [{current:.5},{material:1},{mass:1.5}]){const model=create(settings);all.push(model);const state=run(model);assert.ok(state.blocked&&!state.held);assert.equal(state.loadY,0);assert.equal(state.hoistY,0);if(settings.material===1){assert.ok(state.current>1.9);assert.equal(state.force,0);assert.equal(state.flux,null,'open return path must not claim zero coil field');geometry(model);}}
const reverse=create({polarity:-1});all.push(reverse);const reversed=run(reverse);close(reversed.force,normalDone.force);close(reversed.loadY,normalDone.loadY);assert.equal(reversed.currentDirection,-1);
for(const fps of [30,144]){const model=create();all.push(model);const state=run(model,fps);for(const key of ['current','loadY','hoistY','elapsed','force'])close(state[key],normalDone[key],1e-9,`${fps} Hz result ${key}`);}
const zeroHeight=create({height:0});all.push(zeroHeight);const zero=run(zeroHeight);assert.ok(zero.held);close(zero.loadY,.002);assert.equal(zero.hoistY,0);
// Changing a target while paused cannot move either body. Actual motion resumes with bounded acceleration.
const manual=create();all.push(manual);run(manual);const before=manual.getState();manual.update({height:20});close(manual.getState().loadY,before.loadY);close(manual.getState().hoistY,before.hoistY);run(manual);assert.ok(manual.getState().held);close(manual.getState().hoistY,.02,1e-5);
// Reversal passes through zero. No cached attachment can survive insufficient instantaneous force.
manual.update({polarity:-1});let sawDetached=false,sawNegative=false;for(let i=0;i<500&&!manual.getState().complete&&!manual.getState().blocked;i++){manual.advance(.005);const state=validState(manual);sawDetached||=!state.held;sawNegative||=state.current<0;}assert.ok(sawDetached&&sawNegative);run(manual);geometry(manual);
manual.update({material:1});assert.equal(manual.getState().held,false);assert.equal(manual.getState().force,0);run(manual);assert.equal(manual.getState().loadY,0);
manual.reset();run(manual);manual.update({current:0});let detached=false;for(let i=0;i<500&&!manual.getState().blocked;i++){manual.advance(.01);const state=validState(manual);detached||=!state.held;}assert.ok(detached);run(manual);assert.equal(manual.getState().loadY,0);
// The narrow-gap model must not attach a distant load when a completed lift has already released it.
manual.reset();run(manual);manual.update({operation:1});run(manual);manual.update({operation:0});run(manual);assert.ok(manual.getState().blocked&&!manual.getState().forceValid&&!manual.getState().held);
manual.reset();const fresh=manual.getState();assert.equal(fresh.current,0);assert.equal(fresh.stage,'ready');assert.equal(fresh.loadY,0);assert.equal(fresh.hoistY,0);geometry(manual);
manual.update({current:Infinity,height:NaN,mass:-1});assert.equal(manual.getState().values.current,2);assert.equal(manual.getState().values.height,40);assert.equal(manual.getState().values.mass,.1);
for(const model of all)model.dispose();
console.log('Electromagnet pickup/release, force/current/material controls, moving contacts and wires, actual winding, paused changes and 30/60/144 Hz parity passed.');
